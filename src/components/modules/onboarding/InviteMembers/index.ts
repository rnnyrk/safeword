import styled from 'styled-components/native';

import { ButtonContainer } from 'common/interaction/Button/styled';

export const InviteMemberButtonContainer = styled.View`
  width: 100%;

  ${ButtonContainer} {
    margin-top: 16px;
  }
`;
